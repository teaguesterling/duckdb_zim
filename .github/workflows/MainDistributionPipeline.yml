#
# This workflow calls the main distribution pipeline from DuckDB to build, test and (optionally) release the extension
#
name: Main Extension Distribution Pipeline
on:
  push:
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ github.head_ref || '' }}-${{ github.base_ref || '' }}-${{ github.ref != 'refs/heads/main' && github.sha || '' }}
  cancel-in-progress: true

jobs:
  duckdb-stable-build:
    name: Build extension binaries
    uses: duckdb/extension-ci-tools/.github/workflows/_extension_distribution.yml@v1.5-variegata
    with:
      duckdb_version: v1.5.2
      ci_tools_version: v1.5-variegata
      extension_name: zim
      # Linux builds are excluded: the icu dependency fails to compile on the release-only
      # community triplets (*-linux-release) under CI's gcc-toolset-14 — an upstream
      # vcpkg/icu/toolchain issue (both icu 78.1 and 78.3 fail; the full x64-linux triplet
      # builds icu fine here; the extension builds + passes all tests locally). Re-enable
      # when the upstream build is fixed. See issue #2.
      exclude_archs: 'linux_amd64;linux_arm64'

  code-quality-check:
    name: Code Quality Check
    uses: duckdb/extension-ci-tools/.github/workflows/_extension_code_quality.yml@v1.5-variegata
    with:
      duckdb_version: v1.5.2
      ci_tools_version: v1.5-variegata
      extension_name: zim
      # format only: the tidy check configures CMake without vcpkg, so this extension's
      # required libzim (pkg-config) isn't found and tidy can't even configure.
      format_checks: 'format'
